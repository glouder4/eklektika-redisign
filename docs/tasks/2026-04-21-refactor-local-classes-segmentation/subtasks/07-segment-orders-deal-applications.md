# ST-07: Заявки из сделки B24 и корзина

## Связь с задачей

- Родительская задача: [TASK-2026-04-21-refactor-local-classes-segmentation](../README.md)
- Внешние ссылки:
  - нет

## Цель подзадачи

Вынести функции `getApplication`, `addApplication` и связанный с ними вызов REST `kit.productapplications.deal.productrows.get` из [`local/php_interface/init.php`](../../../local/php_interface/init.php) в отдельный сегмент «заказы ↔ заявки сделки», используя клиент REST из ST-02 и не смешивая с пользовательской синхронизацией CRM.

## Описание работ

1. Оформить класс сервиса (например, `DealApplicationsService`) с методами переноса строк заявок в корзину Sale; вынести вложенные функции `findItem`, `setPrice`, `updateOrder` в приватные методы класса или отдельные маленькие классы.
2. Заменить хардкод вебхука `w8i2ce68y3wwps17` на конфиг из ST-02 (отдельный ключ от общего REST, если это отдельный вход).
3. Найти все вызовы `addApplication`/`getApplication` в проекте и обновить на новый фасад; удалить запись логов в файлы документа корня сайта или сделать логирование конфигурируемым (по согласованию).

## Технические детали

- Компоненты/модули:
  - [`local/php_interface/init.php`](../../../local/php_interface/init.php) — функции `getApplication`, `addApplication`
  - возможные вызовы из интеграций заказов (поиск по имени функции в репозитории)
- Изменяемые файлы/области:
  - `local/php_interface/init.php`
  - новый каталог классов заказов под `local/classes/` или будущий модуль `eklektika.orders.applications`

## Зависимости

- Блокируется:
  - ST-02 (конфиг вебхуков и HTTP)
- Блокирует:
  - финальную зачистку `init.php` в ST-08

## Критерии приёмки

- [ ] При наличии сценария добавления заявок в заказ поведение совпадает с текущим (или задокументированы намеренные исправления)
- [ ] Секреты вебхуков не продублированы в новых файлах строковыми литералами без централизованного конфига

## Проверка

- Unit/интеграционные проверки:
  - мок ответа REST для сборки массива `$itemForOrder`
- Ручной сценарий:
  - Создание/обновление заказа с привязкой к сделке на тестовом стенде

## Документация

- Изученные документы:
  - `docs/features/README.md` (раздел заказы — при отсутствии зафиксировать статус «в разработке»)
- Что обновить:
  - `docs/features/local_classes_segments_and_modules.md` — сегмент заявок
  - при появлении контента — `docs/features/` заказы
- Что создать (если нужно):
  - `docs/features/orders_b24_applications.md` — при выделении как отдельной фичи

## Статус

- planned
