# ST-01: Документирование карты доменов и границ модулей

## Связь с задачей

- Родительская задача: [TASK-2026-04-21-refactor-local-classes-segmentation](../README.md)
- Внешние ссылки:
  - нет

## Цель подзадачи

Зафиксировать в репозитории единый источник правды по сегментам `local/classes`, связям сайт↔Bitrix24 и предлагаемым модулям `eklektika.*`, чтобы последующая реализация не расходилась с планом.

## Описание работ

1. Создать файл `docs/features/local_classes_segments_and_modules.md` по структуре: обзор, таблица сегментов (файлы, события, REST), границы out of scope по arch-rules (без перечисления имён сторонних модулей в карте сегментов), предлагаемые модули, порядок миграции, стратегия `init.php`.
2. Обновить `docs/features/README.md`: добавить пункт в списке документов со статусом «планируется / в работе» по факту готовности.
3. Сверить формулировки с существующими `b24_integration.md` и `company_system.md` — добавить перекрёстные ссылки разделами «См. также», без копирования больших фрагментов кода.

## Технические детали

- Компоненты/модули:
  - документация только; код не меняется
- Изменяемые файлы/области:
  - `docs/features/local_classes_segments_and_modules.md` (создание)
  - `docs/features/README.md` (правка)
  - опционально: «См. также» в `docs/features/b24_integration.md`, `docs/features/company_system.md`

## Зависимости

- Блокируется:
  - нет
- Блокирует:
  - осмысленное выполнение ST-02–ST-09 (как ссылочная база)

## Критерии приёмки

- [x] Файл `docs/features/local_classes_segments_and_modules.md` создан и содержит таблицу сегментов с путями к ключевым файлам
- [x] В `docs/features/README.md` есть ссылка на новый документ
- [x] Явно перечислены дополнительные области взаимодействия (не только три базовых блока): REST-слой в `init.php`, постобработка 1С, заявки сделки↔заказ, поиск (`Stemming`), `PageSettings`, домен `CatalogPriceFloor`

## Проверка

- Unit/интеграционные проверки:
  - нет
- Ручной сценарий:
  - Прочитать новый документ и убедиться, что все пути файлов существуют в репозитории

## Документация

- Изученные документы:
  - `docs/features/README.md`
  - `docs/features/b24_integration.md`
  - `docs/features/company_system.md`
  - карта в [README родительской задачи](../README.md)
- Что обновить:
  - `docs/features/README.md`
  - при необходимости — блоки перекрёстных ссылок в профильных фичах
- Что создать (если нужно):
  - `docs/features/local_classes_segments_and_modules.md`

## Статус

- done
